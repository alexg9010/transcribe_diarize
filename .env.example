# Hugging Face access token (read-only is sufficient)
# Get yours at: https://huggingface.co/settings/tokens
# The installed command falls back to the repo .env if HF_TOKEN is not set globally.
HF_TOKEN=your_token_here
